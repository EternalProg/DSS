{
  "name": "dss-platform-selection",
  "version": "1.0.0",
  "description": "Decision support data registry for alternatives, criteria, and evaluations",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently -n server,vite -c blue,green \"PORT=3001 node --watch server.js\" \"API_PORT=3001 vite\"",
    "build": "vite build",
    "preview": "vite preview --strictPort",
    "seed": "node scripts/seed.js",
    "smoke:consensus": "node scripts/consensus-smoke.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongodb": "^6.7.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "vite": "^7.0.0"
  }
}
